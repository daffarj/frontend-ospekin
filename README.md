
  # Buatkan web dari detail

  This is a code bundle for Buatkan web dari detail. The original project is available at https://www.figma.com/design/y4fF2FDyygHI1Gd2xBHFq4/Buatkan-web-dari-detail.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  